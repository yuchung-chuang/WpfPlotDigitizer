<UserControl x:Class="WpfPlotDigitizer.ErasePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cycControls="clr-namespace:CycWpfLibrary.Controls;assembly=CycWpfLibrary"
             xmlns:local="clr-namespace:WpfPlotDigitizer"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="400">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="auto" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <WrapPanel Orientation="Vertical"
               Margin="20">
      <Button Content="Undo"
              Command="{Binding UndoCommand}" />
      <Button Content="Redo"
              Command="{Binding RedoCommand}" />
    </WrapPanel>
    <cycControls:PanZoomBorder Grid.Column="1">
      <Image Source="{Binding imageSource}" />
    </cycControls:PanZoomBorder>

  </Grid>
</UserControl>
